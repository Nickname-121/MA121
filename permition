# MA121
1. Поделючаемся к вм через коносль с помощью команды ssh root@<IP вм> для того что бы в будущем мы могли копировать ключ в строку
2. Устанавливаем git через команду dnf install git
3. Генерируем ключ через команду ssh-keygen ed25519
4. Чекаем наш ключ через команду cat /root/.ssh/id_ed25519.pub
5. Заходим на GitHub и вставляем наш ключ. Настройки профиля-SSH and GPG keys и вставляем наш ключ в строку.
6. Прописываем команду в консоль git clone <ссылка на гит> что бы настроить репозиторию для работы.
7. Создаем файл README.md в репозиторию с помощью команд:
echo "# MA-121" >> README.md - нужна для вывода текста в строку
git init - инициализирует новый репозиторий GIT
git add README.md - переносит изменения из рабочего каталога в раздел проиндексированных файлов
git commit -m "first commit" - фиксирует изменения в репозитории 
8.Проверяем наличие нашего файла в репозитории на сайте git
9. заходим в README.md через команду vim README.md и пишем туда описание нашей игры.
10. Пушим репозиторий через команды:
git remote add origin <ссылка на гит>
git branch -M main
git push -u origin main
11. Заходим на гит и чекаем что бы в нужном нам файле был наш текст.
12.Далее повторяем шаги с 7 по 11 создавая новый файл "rules" в репозиторий.
13. Создаем 3 файл где пишем все что мы сделали и дальше пушим его как мы делали с предыдущими файлами
 
# MA121
# MA121
